import { styled } from "@/theme";

export const Form = styled('form', {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'stretch',
  gap: '$3',
  maxWidth: '$8',
  color: '$text_body',
  width: '100%',
})

export default Form
